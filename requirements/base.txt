# Core runtime dependencies shared across platforms
# Note: torch is intentionally omitted here and pinned per platform
# (see macos.txt and linux-cuda.txt)
setuptools<=81.0.0
scvi-tools==1.1.2
scArches==0.6.1
scanpy
anndata==0.10.8
# pandas<2.2.2 wheels are only ABI-compatible with numpy<2.0
numpy<2.0
# Range required to satisfy cuml-cu12/cudf-cu12==24.4.* on linux-cuda
pandas>=2.0,<2.2.2
scipy
torch
pynndescent
tqdm
matplotlib
